# Terraform input variables injected as TF_VAR_* (masked, never written to disk).
# prod resolves the sensitive value from a GitHub secret (jq auto-installs in the container).
SECRETS_PROVIDER=github
secret_note=${DEMO_SECRET_NOTE_PROD}
