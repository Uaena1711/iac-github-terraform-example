# Terraform input variables injected as TF_VAR_* (masked, never written to disk).
# prod resolves the sensitive value from AWS Secrets Manager (post-OIDC).
SECRETS_PROVIDER=awssm
secret_note=${iac-github/demo/secret-note}
