# Literal identity (no secret provider) so the pipeline needs only terraform+git in the image.
AWS_ROLE_ARN=arn:aws:iam::377521353143:role/iac-github-oidc-test
AWS_REGION=us-east-1
AWS_STATE_BUCKET=iac-github-tfstate-377521353143-us-east-1
